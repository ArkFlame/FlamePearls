name: ${name}
description: ${description}
author: ${author}
main: ${mainClass}
version: ${version}
url: ${url}
soft-depends: [ PlaceholderAPI ]
commands:
  flamepearls:
    description: "FlamePearls improves general pearl behaviour and adds new mechanics."
    aliases: [ pearls ]